/*
 * @flow
 */

import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  touchable: {},
  gradient: {
    borderRadius: 7,
    paddingVertical: 15,
    paddingHorizontal: 25,
    minWidth: 100,
  },
  text: {
    color: '#fff',
    textAlign: 'center',
  },
});

export default styles;
